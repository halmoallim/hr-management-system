<h1><%= t 'models.goal.Edit goal'%></h1>

<%= render "form", goal: @goal %>

<br>

<div>
  <%= link_to t('links.show this goal'), @goal, class:"btn btn-secondary"%> 
  <%= link_to t('links.back to goals'), goals_path, class:"btn btn-dark" %>
</div>
