<p style="color: green"><%= notice %></p>

<%= render @employee %>

<div>
  <%= link_to t('links.edit this employee'), edit_employee_path(@employee), class:"btn btn-secondary"  %> 
  <%= link_to t('links.back to employees'), employees_path, class:"btn btn-dark"  %>
  <br>
  <br>
  <%= button_to t('links.destroy this employee'), @employee, method: :delete, class:"btn btn-secondary"  %>
</div>
